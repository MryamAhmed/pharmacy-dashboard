/// Request body / query parameter and response key names.
///
/// Per PR guidelines: never inline a raw key string in a data source, model,
/// or navigation call — add a constant here and reference it.
///
/// Empty for now — this template has no routes that pass extra parameters
/// yet. Add constants here as real navigation/query parameters are needed.
class RouteParameters {
  const RouteParameters._();
}
